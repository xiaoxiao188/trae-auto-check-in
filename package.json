{
  "name": "trae-checkin",
  "version": "1.0.0",
  "private": true,
  "description": "Trae daily check-in on Cloudflare Workers",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
