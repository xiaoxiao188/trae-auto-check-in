name = "trae-checkin"
main = "src/index.js"
compatibility_date = "2026-01-01"

# 每天 UTC 00:30 触发 = 北京时间 08:30，错开 08:00 签到高峰
[triggers]
crons = ["40 10 * * *"]

# 明文变量（非敏感信息放这里，部署时随代码一起上传）
# 推送消息中显示的定时说明，改了 crons 后记得同步修改
[vars]
CRON_LABEL = "每天 10:40（北京时间）"
